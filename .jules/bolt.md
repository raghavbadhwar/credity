## 2024-05-22 - Dead Code Discovery
**Learning:** `CredentialCard` component in `BlockWalletDigi` is unused. The dashboard uses `CredentialListItem` or inline cards instead.
**Action:** Remove dead code to reduce bundle size.
