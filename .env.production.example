# CredVerse Production Environment Variables
# Copy this file to .env in each service directory and fill in the values

# =============================================================================
# REQUIRED - Must be the same across ALL services
# =============================================================================

# 64-character random strings for JWT signing
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
JWT_REFRESH_SECRET=

# Comma-separated list of allowed origins (production URLs)
# Example: https://issuer.credverse.io,https://wallet.credverse.io,https://recruiter.credverse.io
ALLOWED_ORIGINS=

# =============================================================================
# OPTIONAL - Service-specific
# =============================================================================

# Database (PostgreSQL)
# Use Railway's database addon or external provider
# DATABASE_URL=postgresql://user:password@host:5432/database

# =============================================================================
# ISSUER SERVICE
# =============================================================================

# 64-char hex key for encrypting issuer private keys
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ISSUER_KEY_ENCRYPTION=

# Blockchain (Sepolia Testnet or Polygon Mainnet)
# SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
# DEPLOYER_PRIVATE_KEY=0x...
# REGISTRY_CONTRACT_ADDRESS=0x...

# Redis (for bulk credential issuance queue)
# REDIS_URL=redis://...

# Email service (Resend)
# RESEND_API_KEY=re_...
# EMAIL_FROM=CredVerse <no-reply@credverse.io>

# IPFS (Pinata)
# PINATA_JWT=...

# =============================================================================
# GATEWAY SERVICE
# =============================================================================

# Google OAuth (optional, for social login)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=https://gateway.credverse.io/api/auth/google/callback

# =============================================================================
# MONITORING (All Services)
# =============================================================================

# Sentry error tracking
# SENTRY_DSN=https://...

# PostHog analytics
# POSTHOG_API_KEY=phc_...
# POSTHOG_HOST=https://app.posthog.com
