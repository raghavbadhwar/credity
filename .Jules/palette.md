## 2026-02-13 - [Component Consistency]
**Learning:** The `CredentialCard` component is a reusable, accessible component that matches the design system, but key pages (`Dashboard`, `ProfilePage`) currently use inconsistent inline implementations.
**Action:** Future work should refactor pages to use `CredentialCard` for consistent behavior and accessibility.
