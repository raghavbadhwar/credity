// App.tsx - Legacy entry point (not used, see router.tsx)
// The application now uses RouterProvider from main.tsx
// Original demo is available at /legacy route

export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-gray-500">
        Application is now using React Router. See /legacy for original demo.
      </p>
    </div>
  );
}
